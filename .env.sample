# Sample Environment Variables Configuration
# Copy this file to create your environment files:
# - server/.env (for development)
# - server/.env.test (for testing)
# - client/.env (for React client)

# Server Environment Variables
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASS=your_database_password
DB_NAME=trainer_timetable_dev
JWT_SECRET=your_jwt_secret_key

# Test Environment Variables
# Use different database name for testing
DB_NAME_TEST=trainer_timetable_test
JWT_SECRET_TEST=your_test_jwt_secret_key

# Client Environment Variables
REACT_APP_API_ORIGIN=http://localhost:5000
